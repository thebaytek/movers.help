Place downloaded GLB/glTF models here:

  box-truck.glb   → from https://skfb.ly/ozpZ9 ("Moving box van" by FLED)
  53ft-trailer.glb → from https://skfb.ly/pqLDQ (53' tractor trailer)

Decimation (to keep browser perf smooth):
  The box truck is 46.5k tris — fine as-is for modern browsers.
  To reduce further: Blender → import GLB → add Decimate modifier
  (ratio ~0.5) → export GLB. Target ~15-25k tris per model.

Both models are paid assets on Fab.com. Purchase, download GLB format,
optionally decimate, drop here. The app falls back to procedural models
until the files exist.
